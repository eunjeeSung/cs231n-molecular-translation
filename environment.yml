name: venv
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pytorch
  - torchvision
  - cudatoolkit=10.2
  - pillow   
  - pip:
    - matplotlib
    - pandas
    - pyyaml
    - scikit-learn
    - scipy
    - seaborn
    - sklearn
    - tqdm  
prefix: /opt/conda/envs/venv
